const entraceIncome = async(Name, FontOfentrance, balance) => {
    return {
        nameUser,
        fontsInput,
        price,
        totalFonts: () => price + price
    }
}

export default {
    entraceIncome,
    
}